---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const notes = await getCollection('notes');
---
<BaseLayout title="首页">
  <div class="page">
    <p style="padding: 80px 0;">Found {notes.length} note(s)</p>
  </div>
</BaseLayout>
